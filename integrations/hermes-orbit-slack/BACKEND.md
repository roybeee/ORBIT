# Canonical Slack directive backend — unreleased candidate

Fresh remote default branch verified against `git ls-remote`: `1739316ef0a2f345c1aab2d935e3b3df283beaea`. Existing task commits preserved. No deployment or real user write has been performed.

## Implemented

`app/api/integrations/slack/directives/route.ts` delegates to the real D1-backed service in `lib/orbit/slack/directives.ts`. It uses canonical `readWorkspace`, `writeCommand`, `readNote`, note revisions, workspace chunks and mutation receipts; no parallel fake note store.

- Only `change.kind=note` is supported, creating a canonical `kind=knowledge` note. Text/order are preserved. Canonical note `updated` holds the supplied date; API readback exposes it as `date` for v2 compatibility. No filesystem KB adapter. Existing `/home/hermes/mealzip-knowledge/projects/old-ferry-donut.md` is neither reread nor rewritten by this service.
- A single integration credential is bound server-side to owner + Slack workspace + requester, with `directives:write` scope, expiry and revocation. Raw credentials are never stored. Neither posted owner IDs nor browser/platform auth headers grant integration authority.
- Trusted transport attestation boundary: the scoped integration caller is responsible for authenticated Slack event provenance. This endpoint does NOT independently prove Slack event authenticity or user approval. Never expose the credential to model inputs or untrusted callers. Gateway envelope/alias defects remain release blockers.
- `GET ?resolveProjectId=...&workspaceId=...&requesterId=...` authorizes the project in the credential owner's actual workspace. Posted v2 binding must match this server-derived authority.
- `POST` persists the immutable bounded wire payload and receipt. Operation identity is owner/workspace/requester/operationKey; sorted-key payload hash rejects conflicting reuse (409). Credential rotation preserving principal does not lose reconciliation access.
- Successful notes and receipts commit in one D1 atomic batch using a new optional canonical repository transaction extension. Credential authority is rechecked in the committing statement; canonical workspace revision prevents project authorization from racing a project edit/delete. Any receipt insert failure rolls back workspace/chunks/note revision/mutation. Duplicate races reconcile the winner.
- `GET ?id=...` or `?operationKey=...` reads canonical note data, not an echoed target in receipt JSON. Edited/deleted targets report `target_changed`/`target_missing`, never completed. A lost commit acknowledgement is reconciled from durable state; a 404 is not permission to blindly retry a provider write.
- Missing/name-only project stores `needs_confirmation` with at most eight owner-scoped canonical project IDs and no note. Explicit unauthorized IDs get 403. Failed provider stores `provider_failed` and no target. No provider network request or calendar outbox entry is made.

## Provisioning and migration (release operator only)

Append-only generated migration: `drizzle/0029_woozy_mikhail_rasputin.sql`, plus `drizzle/meta/0029_snapshot.json` and updated journal. Adds `orbit_slack_credentials` and `orbit_slack_directives`; does not seed credentials or user data. Apply through the canonical approved release migration path, not runtime table creation.

An approved operator must generate a cryptographically random credential (at least 32 random bytes, base64url), retain it only in the existing scoped secret configuration, and provision only its SHA-256 digest with a verified canonical owner ID, verified Slack workspace and requester IDs, literal scope `directives:write`, explicit future epoch-millisecond expiry and `revoked=0`. There is deliberately no unauthenticated registration endpoint or automatic posted-owner enrollment. Neither task context nor this document supplies a verified owner/workspace/channel mapping. Revoke with `revoked=1`; rotation must preserve the verified principal. No provisioning was performed here.

## Verification

`node --experimental-strip-types --test tests/slack-directives.test.mjs` executes the service against real SQLite using all canonical migrations and D1 batch semantics. Covers canonical note/readback, auth/isolation, concurrent duplicate/conflict, failure/ambiguity, rollback, lost ACK and commit-time revocation.

After `npm run build`, `node --experimental-strip-types --test tests/slack-directives-http.test.mjs` exercises the actual built Worker route through HTTP Request/Response with real SQLite and provider fetch explicitly blocked: authorize -> note commit -> canonical readback -> reconcile -> replay; ambiguity returns no target. Identities/credentials are test-only fixtures; responses are generated by the actual backend, not a mock server. This is not a production or real-Slack demo.

## Remaining hard gates / rollback

The local note-only core now supports `orbit_slack_note_prepare`: read-only `GET ?prepareNote=1&workspaceId=...&requesterId=...` resolves up to eight actual owner-scoped canonical candidates (optional exact `projectId`). Name-only input never silently selects a destination. The plugin durably freezes the original note and candidates, prompts for exact requester approval even for one explicit project, and uses the existing authenticated Slack readback + original cancellation fence before execute. Execute reauthorizes the selected project, POSTs once, and verifies the canonical note via GET; uncertain dispatch uses operation-key reconciliation. Preparation makes no backend receipt or note write. The legacy wire's `providerStatus=succeeded` in this note path requests local ORBIT execution; it does not attest to an external provider attempt. Only canonical readback establishes completion.

`tests/test_note_backend.py` uses actual gateway ledger/context, real SDK full discovery and registry dispatch of BOTH exposed tools, and the real TypeScript/SQLite service over a test-only stdin transport. Successful legacy note calls are routed through canonical preparation, not directly to POST. A dispatch-level selected-choice fence denies stale unapproved notes. Already approved identical retries do not require a new approval; failed-provider receipts create no note and preserve their receipt-only behavior. There is no trustworthy new-note destination-intent binding in this candidate beyond verified requester selection; project access and model assertions alone do not establish intent. Slack auth/message readback responses are fixtures, NOT real Slack verification. It proves ambiguity causes zero note/receipt writes, a different requester cannot approve, and the verified requester choice produces one exact-text canonical note with one POST across replay. The built Worker HTTP test separately covers route dispatch.

This is NOT the whole deployed workflow. Legacy backend `needs_confirmation` receipt transitions still return `receipt_transition_required`; do not clear receipts or change operation keys. Preparation avoids that legacy transition by not creating a remote receipt before approval. General provider/classifier orchestration, task/calendar/provider adapters and filesystem KB writes remain unsupported. The backend trusts the scoped caller's approval/provenance attestation, not an independently verified server-side Slack approval token.

User requested production reflection, but parent manages release only after trusted-source queue/alias repairs, verified current channel/message identity, independent QA, CI, approved migration/credential provisioning and authenticated production readback. Do not install this partial candidate as a completed feature.

Rollback: disable/revoke the integration credential and revert source through the normal reviewed path. Keep the two tables, receipt rows and canonical notes; do not drop durable operation keys or replay uncertain work. Migration is additive and safe to retain with older code. Any desired user-data removal is a separate explicitly authorized operation through normal canonical note tools. No production rollback was needed or executed.
