# Release 2026-09-22 · GitHub main 921b3c7

| Item | Value |
|---|---|
| GitHub `main` SHA | `921b3c772d062046bb63c21252efd735743541d5` |
| Source tree | `080f3515806fda8d8f8a723078000a9e5b437bba` |
| Validate Orbit | success (push to main) |
| Sites source push | exact-tree projection `9e859b8eb86e53ae5d6f4e7323e928024cf8ab6d` (tree identical), pushed by `scripts/parallel/publish-sites.sh` via Codex sites-hosting |
| Sites version / deployment | version `115` · `appgdep_6ab268d67b208191b0bd9fb57e6e6de9` succeeded |
| Verified running tree | `080f3515806fda8d8f8a723078000a9e5b437bba` — authenticated same-origin `GET /api/version` in the owner's browser returned `{"build":"2026-09-22T11:36:42.865Z","tree":"080f3515…"}` (200, JSON) |

Content: Hermes `session_meta` transcript rows accepted instead of failing the whole activity source (#45), verified Slack task receipts with legacy project candidates and scoped runtime transport (#40).

Verified in production after the deployment: `GET /api/brief/status` reports `hermes_activity` as `partial` instead of `error` — "형식 확인이 필요한 대화 43개는 분리해 재확인합니다. 나머지 기록은 계속 수집합니다" — and the unconfirmed collection backlog fell from 679 to 577 records while the page was open. Before this release one `session_meta` row aborted every collection run with HERMES_FORMAT 502.

Notes: Codex reported npm ci, build, packaging and 703 tests passing. Its own post-deployment `/api/version` probe returned AUTH because the Sites sign-in wall rejects unauthenticated reads; the owner's browser check above is the verification. Two publish runs were started in parallel by mistake against the same checkout; the first completed the deployment and the second was stopped, and both projected the identical tree.
