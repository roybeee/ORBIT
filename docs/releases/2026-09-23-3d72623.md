# Release 2026-09-23 · GitHub main 3d72623

| Item | Value |
|---|---|
| GitHub `main` SHA | `3d72623ac120dc30c54e60934dce962025a38e24` |
| Source tree | `591cfe573cc836b3d231cfd62a958b86ecaa1de8` |
| Validate Orbit | success — run 35810749620 |
| Sites source push | exact-tree projection `8810026e3fb61ba80c82bc4f9de29d83ba3aff1c` (tree identical), pushed by `scripts/parallel/publish-sites.sh` via Codex sites-hosting |
| Sites version / deployment | `appgver_480d8cc959488191b8c3debca2b278b2` · `appgdep_6ab33bed40e081919c1d3305fbb5f97a` succeeded |
| Verified running tree | `591cfe573cc836b3d231cfd62a958b86ecaa1de8` — authenticated same-origin `GET /api/version` in the owner's browser returned `{"build":"2026-09-23T02:35:46.264Z","tree":"591cfe57…"}` (200, JSON) |

Content: everything merged since `41016f9` — the analysis order change (#62) plus the operations guardrails another session landed (#58 Playwright core-journey smoke, #59 publish quota preflight / publish-clone cleanup / this status board / delegation contract, #60 stale relogin flag warns instead of blocking, #61 a shared Codex+Hermes account warns instead of blocking).

Measured in production after the deployment, with the newest-first order live: a cold full analysis is 1,313 source units plus roughly 190 merge calls. 206 new units took 94.8 minutes (27.6 s/unit, 2.2 units/min), which puts a full pass at about 10 hours and roughly 1,500 Hermes runs. Re-preparing the same run afterwards reported `reused: 263` with 1,050 left, confirming that each accepted unit is banked immediately and that reordering does not re-analyse anything.

Notes: Codex reported npm ci, build, packaging and 732/732 tests passing. Token usage could not be measured: the Hermes gateway records nothing for `/v1/runs` in `state.db` (`sessions` and `session_model_usage` hold no `orbit-` rows, and no usage row appeared during the run). The estimate from the pipeline's own limits is 10-25M tokens for a full pass, against 27.5M for all Hermes usage in the last 30 days.
