# Release 2026-09-22 · GitHub main e45d200

| Item | Value |
|---|---|
| GitHub `main` SHA | `e45d2003de52750e71d4c24f595bd95bac05f2a4` |
| Source tree | `ba4ff32e3fa60eec7a0149c3c4e64c6a62a4663e` |
| Validate Orbit | success (push to main) |
| Sites source push | exact-tree projection `8ab68de96d62f11de24276ce3c8781710d59e3ec` (tree identical) |
| Sites version / deployment | `appgver_2be0a00b5250819186ede43c3379fb46` · `appgdep_6ab212043d348191bcde54d6c50c6678` succeeded |
| Verified running tree | `ba4ff32e3fa60eec7a0149c3c4e64c6a62a4663e` — authenticated same-origin `GET /api/version` in the owner's browser returned `{"build":"2026-09-22T05:26:49.158Z","tree":"ba4ff32e…"}` (200, JSON) |

This deployment replaced the `1a418b6` one recorded alongside it and is the revision production served
when the record was written. The publishing session could not confirm the runtime itself: an unauthenticated
request to `/api/version` returns `401`, and a direct address-bar request returns `{"code":"ORIGIN"}`. The
check has to run as a same-origin `fetch` from an authenticated app page, which is how the tree above was read.

Commits between this revision and later ones are listed in the repository history; `scripts/parallel/publish-sites.sh`
publishes a newer `main` when production should catch up.
