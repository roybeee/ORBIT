# Release 2026-09-24 · GitHub main 69f17dc

| Item | Value |
|---|---|
| GitHub `main` SHA | `69f17dcda72c316b0c3ab62a7ddc536409afd455` |
| Source tree | `f89860885b8586ac48f7d7e4b1b4f820ffa733d0` |
| Validate Orbit | success |
| Sites source push | exact-tree projection pushed by `scripts/parallel/publish-sites.sh` via Codex sites-hosting |
| Sites version / deployment | succeeded (see `publish-reports/`) |
| Verified running tree | `f89860885b8586ac48f7d7e4b1b4f820ffa733d0` — authenticated same-origin `GET /api/version` in the owner's browser matched the GitHub tree |

Content: `completeBrief` now lists the citations that did not resolve instead of only saying that they did not (#67).

## What this release closes, and what it does not

The full cold analysis finished under `34ec890`: 1,313 source units and 390 merges are cached, and the 2026-09-23 brief was published. A re-run for 2026-09-24 reuses all 1,313 source analyses and 1,665 merge analyses and sends nothing new, which is what the incremental pipeline was built for — a day's plan costs the synthesis call rather than ~1,700 runs.

The 2026-09-24 plan itself is still not buildable. Two distinct failures remain at the synthesis stage, both of which end the run:

- `BRIEF_EVIDENCE` from `completeBrief`: the finished plan cites records that are not in `context.evidence`, which is filtered to what was transmitted. The analyses now legitimately mention every record, so this gate and the "analyse everything" pipeline (2026-09-20) disagree about what may be cited. This release names the ids; the run that would have shown them took the other branch instead.
- An unhandled exception: `advanceAgent`'s catch replaces a non-`AgentError` with "응답을 완료하지 못했습니다", discarding the real message. Nothing on this machine can see what it was.

Next step, one small change: preserve the thrown message for non-`AgentError` failures so the second one stops being invisible. Until then the day falls back to the deterministic plan, which is working.
