# Release 2026-09-22 · GitHub main b9b9454

| Item | Value |
|---|---|
| GitHub `main` SHA | `b9b94547dce28e3305138ac99124f0edc22f4dea` |
| Source tree | `15e811ae3b5270108ef061083542e5dd77204ff2` |
| Validate Orbit | success (push to main) |
| Sites source push | exact-tree projection `c87cf1708b2a7656c1b7278a00dad2679d96384d` (tree identical), pushed by `scripts/parallel/publish-sites.sh` via Codex sites-hosting |
| Sites version / deployment | `appgver_2c1518878ee481918876d71f19d2ebb7` · `appgdep_6ab252c4c9308191831b024885177521` succeeded |
| Verified running tree | `15e811ae3b5270108ef061083542e5dd77204ff2` — authenticated same-origin `GET /api/version` in the owner's browser returned `{"build":"2026-09-22T10:01:58.694Z","tree":"15e811ae…"}` (200, JSON). `verify-deploy.sh` cannot verify this app: an unauthenticated `curl` of `/api/deployment-health` is answered by the Sites sign-in wall, so the script reports `status invalid` whatever the token is. |

Content: Slack directive receipts and the note prepare/approval backend (#35) with migrations 0029 and 0030, meeting cards merged into already registered tasks and events (#33), incremental analysis cache and the three-step plan status (#41) with migration 0031, lint and contract fixes (#31, #39), publish-sites standalone clone (#36), release and rule documentation (#34, #37, #42).
First deployment that carries the Slack directive routes; until now the live app had no such route, so the Hermes plugin could never reach it.

Notes: Codex reported npm ci, build, typecheck, packaging and 693 tests passing before the deployment. Published unattended from a Claude Code session at the owner's request; the publication itself still requires the owner to run the command because Codex holds the Sites connector.
