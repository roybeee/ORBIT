# Release 2026-09-25 · GitHub main 4bb5318

| Item | Value |
|---|---|
| GitHub `main` SHA | `4bb5318dae4c7b29b5aaba71f5806c77a383ed49` (PR #120: Slack "오늘 할 일"을 ORBIT에서 조회하는 `orbit_slack_today`) |
| Source tree | `bcf024ca59e52ba364adaf39fc915633d1d27bfe` |
| Validate Orbit | success · https://github.com/roybeee/ORBIT/actions/runs/36099166466 |
| Sites source push | `publish-sites.sh` (Codex) · Sites commit `889ab14dde6c3bae6f8b7e89c2191a2b70293014` · [report](publish-reports/4bb5318.md) |
| Source state | `merged` |
| Sites version / deployment | `published` · version `appgver_a0ee59f234f881919880feb607c3b7ca` · deployment `appgdep_6ab6091fee788191bd9df9f8dfb97351` succeeded (2026-09-25 KST) |
| Verified running tree | `runtime-verified`: owner browser same-origin `GET /api/version` reported tree `bcf024ca59e52ba364adaf39fc915633d1d27bfe`, build `2026-09-25T05:38:59.789Z`. |
| Hermes plugin | `integrations/hermes-orbit-commands` v2.1.0 deployed with `deploy.sh` (server tests 14/14, gateway registers 5 tools incl. `orbit_slack_today`); backup `~/.hermes/plugin-backups/orbit-slack-directive-sync.bak-20260925-144101` |

Production checks (real):
- Hermes server → `GET …/commands?today=1` with the provisioned Slack credential: 200, counts today 13 / overdue 12 / doing 1 / open 24, 10 items.
- Slack #비서실 "오늘 ORBIT 할 일 몇 개야?" → one bot message with the counts and ORBIT link in ~30 s (before: ~2 min, "확인 불가" via Aside/Google Tasks).

Same session, Hermes server configuration (not in this repository): Slack delivery reduced to one message per command (`display.platforms.slack`, `native_task_cards: false`), Google OAuth re-consented (refresh token had been revoked), `cron.wrap_response: false`, and `executive-scheduling`/`calendar-task-operations` skills ask a missing duration with 30분/1시간/2시간 buttons and report per destination. Recorded in the owner's memory, not here.
