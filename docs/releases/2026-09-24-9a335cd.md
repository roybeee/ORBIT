# Release 2026-09-24 · GitHub main 9a335cd

| Item | Value |
|---|---|
| GitHub `main` SHA | `9a335cdebfab88f682a59b7e150881a00b7ff958` |
| Source tree | `ad245e0cc19e332b3f5060390414a20cf5990b7b` |
| Validate Orbit | passed · real · PR #87 head `c4dfcb63ee38c1efc6dc28585f87a730cf00bf88`, [run 35992645531](https://github.com/roybeee/ORBIT/actions/runs/35992645531) |
| Source state | `merged` · GitHub `main` verified at `9a335cdebfab88f682a59b7e150881a00b7ff958` |
| Sites source | exact-tree projection `5c3ba3e382ad04dcabcd9111913afc6d62cef126` (parent `8e41936f17f25157431c4f5630a885b8c65d3498`); source tree matches GitHub |
| Sites version | `appgprj_6a9d00d3209481919c9626ca51014318~appgver_b663ebac79c8819186aee987eb93be9d` (version 134) |
| Deployment | `published` · `appgdep_6ab50c0932a88191ac992d09b7650ee5` succeeded at 2026-09-24T11:40:20Z · [ORBIT](https://orbit-personal-os.hflameb.chatgpt.site) |
| Running tree | `blocked` · real production tree was not read; `runtime-verified` is **not claimed** |

## Publication checks

- GitHub `main` and requested commit/tree: passed · real. Source SHA and tree were compared with GitHub Git data and locally fetched Git objects.
- Build: passed · real. `npm run install:ci` completed, `build-site.mjs` completed, and the build's 754 tests passed with 0 failures. The build regenerated a tracked ZIP with different metadata; it was restored in the source checkout and the packaged copy was synchronized to the committed source asset. The pushed source remained clean at the exact GitHub tree.
- Sites source push and archive: passed · real. The workflow confirmed the pushed commit and packaged the build output; Sites saved version 134 and returned deployment `succeeded`.
- Documentation PR checks: passed · real. `npm run typecheck` passed and `npm test` passed (754 application tests, 0 failures; Python-less regression stages also passed). `git diff --check` passed.
- `scripts/parallel/verify-deploy.sh 9a335cdebfab88f682a59b7e150881a00b7ff958`: blocked · real. `ORBIT_RELEASE_HEALTH_TOKEN` is configured as a masked Sites secret but is absent from this machine, so the command was not invoked and has no output to attach. The attempted authenticated browser flow could not complete a production response because the browser's URL security policy blocked the redirect. No `/api/version` tree was observed.

To finish runtime verification, run the repository's `verify-deploy.sh` in an environment that already has the release health token, or obtain an authenticated same-origin `/api/version` response. Record the returned `status`, `build`, and `tree` here, and mark `runtime-verified` only if `tree` equals `ad245e0cc19e332b3f5060390414a20cf5990b7b`.
