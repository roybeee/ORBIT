# Release 2026-09-22 · GitHub main b0c9c58

| Item | Value |
|---|---|
| GitHub `main` SHA | `b0c9c589c067d87a09669061db6477298655112b` |
| Source tree | `f52deca2359fd43af93e23fbe69cc9ab291ae15e` |
| Validate Orbit | success — https://github.com/roybeee/ORBIT/actions/runs/35734881952 |
| Sites source push | exact-tree projection `f8ff0606328897a3867f00d3b79696a213c82e21` (tree identical), pushed by `scripts/parallel/publish-sites.sh` via Codex sites-hosting |
| Sites version / deployment | version `119` · `appgdep_6ab28768832c8191b1b4054d193474fb` succeeded |
| Verified running tree | `f52deca2359fd43af93e23fbe69cc9ab291ae15e` — authenticated same-origin `GET /api/version` in the owner's browser returned `{"build":"2026-09-22T13:47:07.378Z","tree":"f52deca2…"}` (200, JSON) |

Content: an expired quarantine is retried immediately instead of waiting for the session cursor to move (#52).

Verified in production after the deployment: `GET /api/brief/status` reports `hermes_activity` as `partial` with 37 quarantined conversations, down from 43 at release `921b3c7`, and names the grouped reason ("Hermes 메시지 역할을 확인하지 못했습니다.", 37 records) that #47 and #48 added. The remaining quarantine is a single unrecognised-role shape, not a spread of causes.

Also observed while verifying, and not caused by this release: the 2026-09-23 plan analysis (`turn 7e4d0909`) failed at 12:20Z because Hermes's Codex provider returned `429 quota exhausted (retry after 393141s ≈ 4.5 days)`. Source collection is unaffected; plan generation stays unavailable until the Mac's Hermes provider credentials or model routing are changed.

Notes: Codex reported npm ci, build, packaging and 709/709 tests passing. Its own post-deployment `/api/version` probe returned 401 because the Sites sign-in wall rejects unauthenticated reads; the owner's browser check above is the verification. This record also backfills the three preceding deployments of the same evening (`1935840`, `4b7a2d4`, `cd2d439`), which were published but never written down.
