# Claude instructions for ORBIT

Read and follow [`AGENTS.md`](AGENTS.md) before making any change.

This also applies when a Slack or Hermes task invokes Claude. Refresh the GitHub checkout and read the current instructions for every task; previous session context and Sites versions are not source-of-truth references.

The canonical source is the current remote `main` branch of `roybeee/ORBIT`. Always fetch and verify `origin/main`, then create a new work branch from that exact remote SHA. Never use `archive/main-76769c8` as a development base unless the owner explicitly reverses the decision.
